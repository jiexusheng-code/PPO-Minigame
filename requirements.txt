# Requirements for developing and running the minigame PPO experiments
# Pins chosen to avoid OpenMP/runtime conflicts and stay compatible with pysc2/protobuf
stable-baselines3==2.3.0
sb3-contrib==2.3.0
gymnasium==0.29.1
# PyTorch packages are installed via conda (pytorch channel) for platform-specific wheels
# Remove torch/torchvision/torchaudio from pip requirements to avoid missing-wheel errors
pysc2==3.0.0
protobuf>=3.20,<4.0
absl-py
opencv-python>=4.8.0
tensorboard
matplotlib
numpy<2.0
pyyaml
